export const metadata = {
    title: 'Signing out',
    description: '',
};

import { SignOut } from '@the/makewith/react/ui';
export default function SignOutPage() {
    return (
        <SignOut />
    );
}